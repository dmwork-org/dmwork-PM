---
title: 表关系图（ER Diagram）
type: reference
project: dmworkim
version: "0.1.0"
created: 2026-03-19
updated: 2026-03-19
authors: [戏精]
status: draft
tags: [octo, dmworkim, database, erd, mermaid]
---

# 表关系图（ER Diagram）

[[概览|← 返回概览]]

## 核心关系图

```mermaid
erDiagram
    user {
        VARCHAR uid PK
        VARCHAR name
        smallint robot
        VARCHAR category
        VARCHAR role
        VARCHAR app_id FK
    }
    
    app {
        VARCHAR app_id PK
        VARCHAR app_key
        VARCHAR app_name
    }
    
    robot {
        VARCHAR robot_id PK "= user.uid"
        VARCHAR app_id FK
        VARCHAR creator_uid FK
        VARCHAR bot_token
        VARCHAR im_token_cache
        VARCHAR bot_commands
    }
    
    robot_menu {
        bigint id PK
        VARCHAR robot_id FK
        VARCHAR cmd
        VARCHAR remark
    }
    
    user_api_key {
        bigint id PK
        VARCHAR uid FK
        VARCHAR api_key
        VARCHAR space_id FK
    }
    
    group {
        VARCHAR group_no PK
        VARCHAR creator FK
        VARCHAR space_id FK
        smallint group_type
    }
    
    group_member {
        VARCHAR group_no FK
        VARCHAR uid FK
        smallint role
        smallint robot
    }
    
    group_setting {
        VARCHAR uid FK
        VARCHAR group_no FK
        smallint mute
        smallint top
    }
    
    space {
        VARCHAR space_id PK
        VARCHAR creator FK
        VARCHAR preset_group_ids
    }
    
    space_member {
        VARCHAR space_id FK
        VARCHAR uid FK
        smallint role
    }
    
    space_invitation {
        VARCHAR invite_code PK
        VARCHAR space_id FK
        VARCHAR creator FK
    }
    
    message {
        VARCHAR message_id PK "webhook 分片"
        VARCHAR channel_id
        smallint channel_type
        VARCHAR from_uid FK
    }
    
    message_extra {
        VARCHAR message_id PK
        VARCHAR channel_id
        VARCHAR from_uid FK
        smallint revoke
        TEXT content_edit
    }
    
    reaction_users {
        VARCHAR message_id FK
        VARCHAR uid FK
        VARCHAR emoji
    }
    
    channel_setting {
        VARCHAR channel_id PK
        VARCHAR parent_channel_id
    }
    
    event {
        integer id PK
        VARCHAR event
        smallint status
    }

    user ||--o{ robot : "robot_id = uid"
    user }o--|| app : "app_id"
    robot }o--|| app : "app_id"
    robot ||--o{ robot_menu : "robot_id"
    user ||--o{ user_api_key : "uid"
    space ||--o| user_api_key : "space_id"
    user ||--o{ group_member : "uid"
    group ||--o{ group_member : "group_no"
    user ||--o{ group_setting : "uid"
    group ||--o{ group_setting : "group_no"
    space ||--o{ group : "space_id"
    user ||--o| space : "creator"
    space ||--o{ space_member : "space_id"
    user ||--o{ space_member : "uid"
    space ||--o{ space_invitation : "space_id"
    user ||--o{ message : "from_uid"
    message ||--o| message_extra : "message_id"
    message ||--o{ reaction_users : "message_id"
```

## 关键关系说明

### Bot = 特殊 User

Bot（机器人）在系统中是 `user` 表中 `robot = 1` 的特殊用户：

```
user.uid → robot.robot_id（一对一）
user.robot = 1（标记为机器人）
user.app_id → app.app_id（所属 App）
robot.creator_uid → user.uid（Bot 的创建者）
```

### Space 多租户隔离

```
space
  ├── space_member（成员体系）
  │     └── role: 0=成员, 1=管理员, 2=拥有者
  ├── group.space_id（群组归属）
  └── space_invitation（邀请系统）
```

### 消息分片

```
webhook 模块（message, message1~4）
  └── 按 message_id % 5 路由
      └── 每张表结构完全相同
```

### Channel 关系

Channel 不是独立的表，而是通过 `(channel_id, channel_type)` 复合键在各表中引用：

```
channel_type:
  1 = 个人频道（DM）
  2 = 群组频道
  3 = 客服频道
  5 = 社区频道
  6 = 话题频道（带 parent_channel_id）
```

## 用户与好友关系

```mermaid
erDiagram
    user {
        VARCHAR uid PK
        VARCHAR name
    }
    
    user_setting {
        VARCHAR uid FK
        VARCHAR to_uid FK
        smallint mute
        smallint top
        smallint blacklist
        VARCHAR remark
        smallint flame
    }
    
    apply {
        bigint id PK
        VARCHAR uid FK
        VARCHAR to_uid FK
        smallint status
    }

    user ||--o{ user_setting : "uid → 设置对象"
    user ||--o{ apply : "uid → 申请人"
    user ||--o{ apply : "to_uid → 被申请人"
```

`user_setting` 是用户关系的核心表（每对用户关系一条记录），包含：
- 好友关系（通过是否存在记录判断）
- 黑名单（`blacklist = 1`）
- 免打扰、置顶等个性化设置
- 阅后即焚设置（`flame`, `flame_second`）

---

## 相关文档

- [[概览]] — 数据库整体概览
- [[表结构详情]] — 完整字段定义
- [[消息分片设计]] — 分片策略详解
- [[Bot数据模型]] — Bot 相关表

---

## CHANGELOG

| 版本 | 日期 | 变更 |
|------|------|------|
| 0.1.0 | 2026-03-19 | 初稿，Mermaid ER 图 |
